# AI Coach Lab：P02 回答策略版本更新指南

> 更新主题：P02「时间点与时长查询」从单字段查询升级为结构化回答规划链路  
> 本地项目：`D:\download\ai-coach-lab-v0.1`  
> 实施日期：2026-08-23  
> 当前状态：本地实现完成，自动化测试和真实 DeepSeek 调用已通过  
> 安全说明：`.env`、API Key、运行历史等本地敏感信息不纳入 Git 仓库

---

## 1. 这次更新解决了什么

更新前，P02 的基本做法是：Router 识别字段，Python 取出单个值，Prompt 把检索结果交给模型。回答因此很容易变成：

```text
根据您的数据，您在2026年5月6日的入睡时间是23:31。原始数据未提供时区。
```

它有四个产品问题：

1. 只返回一个孤立字段，没有完成用户对这一晚的最小理解；
2. 具体年月日、时区缺失等调试信息进入了用户回答；
3. 模型可能自由补充无依据的评价、因果或医学结论；
4. 不同字段缺少可复用的回答策略，只能继续堆 Prompt 或手写模板。

更新后，P02 的链路变为：

```text
用户原始问题
→ LLM Router 归一为 P02 + 标准 metric
→ Retrieval 取得当前事实、默认相邻事实和个人历史
→ Insight Engine 计算个人比较、显著性和受控候选关系
→ Answer Planner 形成结构化 Answer Plan
→ DeepSeek 基于 Answer Plan 生成整段自然语言
→ Response Validator 校验事实、话术和边界
→ 不合格时携带具体错误原因让 DeepSeek 重写一次
→ 最终回答
```

关键职责没有混在一起：

- 产品通过 JSON 决定指标族、默认信息包、展示门槛、允许关系和表达边界；
- Python 负责取数、计算、过滤、规划和校验，不拼接最终睡眠回答；
- DeepSeek 负责理解用户措辞和生成自然的完整表达，但只能使用 Answer Plan 提供的个人事实及关系。

---

## 2. P02 的四个指标族与默认信息包

| 指标族 | 标准字段 | 默认补充内容 |
|---|---|---|
| 睡眠边界 | `bedtime_start`、`bedtime_end` | 另一个边界 + 总睡眠时长 |
| 总体时长 | `total_sleep_duration`、`time_in_bed` | 睡眠区间，或实际睡眠 + 非睡眠差值 |
| 睡眠阶段 | `deep_sleep_duration`、`rem_sleep_duration` | 该阶段占总睡眠比例 |
| 连续性时长 | `awake_time`、`latency` | 历史充分时补充个人近期比较 |

这意味着不需要为“几点睡”“昨儿什么时候睡着”“昨天晚上几点入睡”分别维护答案。Router 将表达归一为标准字段，后面的产品策略只面向字段和指标族。

`我是不是很晚才睡？` 不属于纯同义问法：它还要求“早/晚判断”。本版将它留作 Router/Bad Case 后续迭代，不将其硬编码成单纯的 `bedtime_start` 查询。

---

## 3. 新增文件

### 3.1 `backend/answer_planner.py`

用途：新增 P02 回答规划层。

主要职责：

- 读取 P02 指标画像、关系规则和表达规则；
- 根据 Router 输出的标准 `metric` 命中指标族；
- 组装 `required_facts`；
- 按指标族组装 `default_companions`；
- 只保留已经通过程序过滤的候选洞察；
- 形成 `answer_policy`，供 DeepSeek 生成和 Validator 校验共同使用。

输出的是结构化 Answer Plan，不是最终用户话术。

### 3.2 `backend/response_validator.py`

用途：新增 P02 生成后确定性校验层。

当前检查：

- 必答字段的展示值是否出现；
- 默认事实型信息包是否完整；
- 是否出现“根据您的数据”等报告腔；
- 是否输出具体年份、时区缺失或内部字段；
- 是否出现配置中禁止的因果或医学结论；
- 是否出现 Answer Plan 未提供的“比较规律”“还算平稳”“正常/异常”“达标”等状态评价。

校验失败后，错误列表会重新进入 P02 Prompt，允许 DeepSeek 完整重写一次。

### 3.3 `config/metric_profiles.json`

用途：把 P02 的产品回答合同从 Python 逻辑中抽出来，成为可配置的指标画像。

包含：

- 四个指标族；
- 八个 P02 标准字段；
- 每个字段的默认信息包；
- 可使用的候选关系类型；
- 比较方式；
- 产品最低展示门槛；
- 测量限定语；
- 基线窗口和最少有效样本数。

这是产品经理后续最重要的可配置文件之一。

### 3.4 `config/metric_relationships.json`

用途：规定“哪些数据关系可以进入候选，以及最多允许说到什么程度”。

示例：

- 可以陈述入睡变晚、醒来稳定、总睡眠减少共同出现；
- 可以说减少的睡眠主要落在更晚入睡这一段；
- 不可以把它扩写成晚睡导致效率、HRV或恢复下降；
- P02 不默认使用跨域关系和医学解释关系。

### 3.5 `config/answer_style_rules.json`

用途：集中维护 P02 的 ToC 表达规则和生成重试策略。

包含：

- 回答深度对应的最大洞察数；
- 使用“你”；
- 禁止的报告腔开场；
- 默认隐藏的具体日期、时区和内部字段；
- 禁止结论片段；
- 无依据状态评价词；
- 生成失败后的重写次数。

### 3.6 新增的六条 P02 Bench Case

新增目录：`bench/p02/`

| 文件 | Case ID | 覆盖字段 |
|---|---|---|
| `bedtime_start.json` | `SLEEP_P02_004` | 入睡时间 |
| `bedtime_end.json` | `SLEEP_P02_005` | 醒来时间 |
| `rem_duration.json` | `SLEEP_P02_006` | REM 时长与占比 |
| `awake_time.json` | `SLEEP_P02_007` | 清醒时长与个人比较 |
| `latency.json` | `SLEEP_P02_008` | 入睡潜伏期与个人比较 |
| `time_in_bed.json` | `SLEEP_P02_009` | 卧床时长、实际睡眠和差值 |

加上原有总睡眠标准/口语、深睡时长三条，P02 当前共有九条 Bench Case，覆盖八个标准字段。

### 3.7 `.gitignore`

用途：阻止公开仓库收录秘密和本地敏感内容。

明确排除：`.env`、`.venv`、Python缓存、`data/raw`、`data/normalized`、`data/runs`、包含本地 Trace 与会话标识的 `过程展示` 文档，以及编辑器临时文件。`过程展示` 仍完整保留在本机，不进入公开仓库。

### 3.8 `data/demo/synthetic_sleep_sessions.csv`

用途：提供不包含用户真实健康数据的合成睡眠数据，让公开仓库在没有私有数据时仍能运行、测试和演示。

该数据只用于工程 Demo，不代表真实用户、医学分布或产品阈值依据。

---

## 4. 已有 Python 文件的改动

### 4.1 `backend/data_service.py`

原本实现：

- P02 主要调用 `get_time_duration_metric()`；
- 只返回目标夜的单个时间点或时长；
- 没有为 P02 专门准备相邻字段、个人历史中心和个人波动范围。

改动部分：

- 新增 P02 字段元数据；
- 新增跨午夜时钟分钟计算；
- 新增最短时钟差计算，避免 23:50 与 00:10 被错误算成相差 23 小时；
- 新增时钟型基线和数值型基线计算；
- 新增 `get_p02_context()`，一次返回当前夜、近七次主睡眠、中心值、MAD、有效样本和内部来源元数据。
- 新增 `resolve_sleep_data_path()`：本机优先读取被 Git 忽略的私有标准化数据；找不到时回退到仓库安全的合成 Demo 数据。

改动目的：

让后续模块拿到的是“生成正确回答所需的结构化上下文”，而不只是一个孤立值；同时由程序完成算数，避免 LLM 自行计算。

### 4.2 `backend/retrieval.py`

原本实现：

- P02 返回 `metric`、`value`、`unit` 和 `display_value`；
- 不知道指标族，也没有默认信息包所需事实。

改动部分：

- P02 改为调用 `get_p02_context()`；
- 读取 `metric_profiles.json` 并附上 `metric_profile`；
- 形成 `current_facts`；
- 计算深睡/REM占比和卧床—实际睡眠差值；
- 保留原来的顶层 `value`、`unit`、`display_value`，保证旧调用兼容。

改动目的：

让 Retrieval 输出既能满足原有单值查询，又能支持默认信息包和后续候选洞察。

### 4.3 `backend/insight_engine.py`

原本实现：

- 主要为 P01/P05/P14 生成洞察；
- P02 没有独立的显著性与关系计算。

改动部分：

- 新增 P02 显示值和时间比较辅助函数；
- 新增 `build_p02_analysis()`；
- 对当前值和个人历史中心进行比较；
- 显著性阈值取“产品最低展示门槛”和“2×个人 MAD”中更严格者；
- 只有有效历史样本达到配置要求才允许个人比较；
- 根据 `metric_relationships.json` 组合受控候选；
- 候选只包含事实、证据强度、允许表达和禁止表达，不生成用户话术。

改动目的：

把“往哪个方向洞察、什么才值得展示、能说到什么程度”从 LLM 自由发挥改为产品可控、程序可追踪。

### 4.4 `backend/prompts.py`

原本实现：

- P02 没有专属的回答合同；
- 模型容易复述具体日期和“原始数据未提供时区”；
- Prompt 接收检索结果后有较大的自由组织空间。

改动部分：

- 新增 `P02_COACH_SYSTEM`；
- `coach_messages()` 新增 `answer_plan` 和 `validation_feedback` 参数；
- P02 的 user message 只包含原始 query 和结构化 Answer Plan；
- 明确要求先回答用户所问、整合默认信息包、最多一条候选、使用相对日期和“你”；
- 明确禁止内部元数据、医学判断、确定因果、虚构事件和无依据状态评价；
- 支持携带 Validator 错误原因重新生成。

改动目的：

最终自然语言仍由 DeepSeek 完整生成，但生成空间被限制在产品批准的事实和关系中。

### 4.5 `backend/coach.py`

原本实现：

```text
retrieval → insight → prompt → LLM → final
```

改动后 P02：

```text
retrieval
→ P02 analysis
→ answer_planner
→ prompt_builder
→ llm_generation
→ response_validator
→ 必要时重写一次
→ final_response
```

其他改动：

- P02 加入需要洞察计算的原型集合；
- 新增 Answer Plan 和 Validator 的 Trace 输入输出；
- 安全拦截、策略拒答、检索失败时正确跳过新增节点；
- 非 P02 原型保持原流程，并把两个新节点标记为 skipped。

改动目的：

把 P02 的产品策略真正接入运行主链路，并让网页可以点击查看每一步实际输入与输出。

### 4.6 `backend/evaluator.py`

原本实现：

- Retrieval Score 可检查目标字段和值是否存在；
- 无法检查 P02 是否命中正确指标族和默认信息包。

改动部分：

- `retrieval_score()` 支持 `metric_family`；
- 支持逐项检查 `default_companions`。

改动目的：

Evaluation 不再只评“有没有查到那个值”，还评“有没有执行正确的回答策略”。

---

## 5. 已有 JSON、测试和文档的改动

### 5.1 `config/workflow_modules.json`

原本：工作流没有回答规划和生成后校验节点。

现在：

- 新增 `answer_planner`；
- 新增 `response_validator`；
- 更新 Insight、Prompt 和资源目录的文件映射；
- Workflow Studio 可以直接查看两个新节点的 input、output、meta 和源码。

### 5.2 `config/product_decisions.json`

新增三条临时产品决策：

- `PD-04`：P02 默认信息包；
- `PD-05`：个人变化展示门槛；
- `PD-06`：程序与 LLM 的生成职责。

这些决策均标记为 `provisional`，便于后续用真实数据、用户研究和 Bad Case 校准，而不是把当前方案伪装成最终医学口径。

### 5.3 `config/rubrics/p02_time_duration.json`

原本：四条较基础的时间与事实 Rubric。

现在：扩展为六条正向标准和三条负向扣分规则，覆盖：

- 首先直接回答；
- 默认信息包完整；
- 时长单位和阶段占比；
- ToC 自然表达；
- 候选洞察 Grounding；
- 隐藏内部元数据；
- 虚构事实、医学结论和确定因果的重罚。

### 5.4 原有三条 P02 Bench Case

修改文件：

- `bench/p01/sleep_duration_standard.json`
- `bench/p01/sleep_duration_colloquial.json`
- `bench/p01/deep_sleep_standard.json`

原本：`expected_retrieval` 只检查 `metric`。

现在：增加 `metric_family`、`default_companions` 和 `PD-04/05/06` 决策引用。

### 5.5 `tests/test_core.py`

新增或增强的测试覆盖：

- 睡眠边界默认信息包；
- REM时长与占比；
- 基于受控事实生成时间构成候选；
- 全链路出现 Answer Plan 和 Validator 节点；
- 首次生成失败后触发一次重写；
- 拒绝报告腔、时区元数据和具体年份；
- 拒绝无依据的“比较规律/还算平稳”等状态评价；
- Retrieval Score 检查指标族和默认信息包。

### 5.6 `docs/P02_回答策略规格_v0.1.md`

原本：产品讨论稿，程序落点标记为尚未实施；并错误地把“我是不是很晚才睡”列为纯时间点同义问法。

现在：

- 标记基础链路已经实施；
- 明确该问法包含个人判断诉求，不等同于纯 `bedtime_start` 查询；
- 程序落点和验收章节更新为实施后的复盘口径；
- 阈值、基线窗口等仍保留为待验证产品决策。

### 5.7 `ARCHITECTURE.md`

新增 P02 Answer Planner 和 Response Validator，解释 Python、配置和 LLM 的职责边界，以及四个指标族如何复用回答策略。

### 5.8 `README.md`

更新主运行链路、P02 Bench 数量和新增的产品可配置文件。

---

## 6. 网页中会看到什么变化

运行一次 P02 问题后，中间工作流会多出两个真实业务节点：

### Answer Planner

输入主要显示：

```text
query
metric_profile
baseline_comparisons
insight_candidates
```

输出完整显示：

```text
required_facts
default_companions
candidate_insights
answer_policy
```

### Response Validator

输入显示首版回答和 Answer Plan；输出显示：

```text
initial.valid / errors
retry（如果发生）
final.valid / errors
```

因此可以在面试或团队评审中明确演示：一条 P02 回答不是“扔给大模型自由回答”，而是经过了策略规划、模型生成和确定性校验。

---

## 7. 验证结果

### 7.1 自动化测试

运行命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：`22 tests`，全部通过，其中包含私有数据缺失时自动回退到合成 Demo 的测试。

### 7.2 Streamlit 启动级测试

使用 Streamlit AppTest 加载 `app.py`，结果：`0 exceptions`。

### 7.3 真实 DeepSeek 路由测试

九条 P02 Bench Case 全部得到：

```text
正确 prototype = P02
正确标准 metric
Routing Score = 1.000
```

覆盖：入睡、醒来、总睡眠、卧床、深睡、REM、清醒、入睡潜伏期和口语表达。

### 7.4 真实 DeepSeek 端到端回答测试

四个指标族各选一个代表问题完成 Router → Retrieval → Insight → Answer Plan → DeepSeek → Validator 全链路。

通过后的代表回答：

```text
你昨晚大约23:31睡着的。之后一觉睡到早上7:42醒来，总共睡了大约7小时9分钟。

你昨晚总共睡了大约7小时9分钟。昨晚23:31左右入睡，今早7:42左右醒来。

你昨晚的REM睡眠大约有1小时10分钟，约占整晚睡眠的16%。

你昨晚睡后清醒时间大约是1小时2分钟，比此前最近7个有效主睡眠的平均水平（1小时15分钟）少了13分钟左右。
```

真实测试曾发现模型擅自补充“整体节奏比较规律”“整体还算平稳”。该 Bad Case 已转化为配置规则、Validator 检查和自动化测试；修复后重新调用通过。

---

## 8. 运行方式

在 PowerShell 中：

```powershell
Set-Location 'D:\download\ai-coach-lab-v0.1'
.\.venv\Scripts\python.exe -m streamlit run app.py
```

浏览器打开：

```text
http://localhost:8501/
```

建议依次测试：

```text
我昨晚几点睡着的？
我今天几点醒的？
我昨晚睡了多久？
我昨晚在床上待了多久？
我昨晚深睡了多久？
我昨晚REM睡眠有多久？
我昨晚清醒时间是多少？
我昨晚躺下后多久才睡着？
```

---

## 9. 产品经理接下来主要修改哪里

| 想调整的产品问题 | 优先修改位置 |
|---|---|
| 每个指标默认多回答什么 | `config/metric_profiles.json` 的 `default_companions` |
| 什么变化值得展示 | `minimum_noticeable_change`、基线样本配置 |
| 哪些关系可以成为洞察 | `config/metric_relationships.json` |
| 每类关系最多允许说到什么程度 | `allowed_claim` / `forbidden_claims` |
| 回答语气、禁止开场、隐藏信息 | `config/answer_style_rules.json` |
| DeepSeek 如何组织最终表达 | `backend/prompts.py` 的 `P02_COACH_SYSTEM` |
| 满分回答如何评分 | `config/rubrics/p02_time_duration.json` |
| 哪些用户问法是好例子或 Bad Case | `bench/p02/*.json` 及后续新增 Case |

一般不建议产品直接修改：

- `backend/data_service.py` 的时间与基线计算；
- `backend/insight_engine.py` 的显著性算法；
- `backend/response_validator.py` 的事实匹配代码；
- `backend/coach.py` 的运行编排。

这些属于工程实现层，产品应先改规格或 JSON，再由开发调整实现和测试。

---

## 10. 当前边界与下一步

当前已经能作为可运行 MVP 和面试项目演示，但仍有明确边界：

1. P02 阈值是产品展示阈值，不是医学阈值，仍需真实数据分布和用户研究校准；
2. 当前基线使用此前最近七次主睡眠，中位数/MAD 规则仍需验证窗口和特殊夜晚剔除；
3. Validator 以确定性数值和词组规则为主，不能替代完整的医学、法规审核；
4. “我是不是很晚才睡”这类查询 + 判断的复合意图，需要后续 Router 和原型编排 Bad Case 迭代；
5. Answer Plan 当前只接入 P02，后续可抽象给 P01/P05 等原型复用，但各原型仍需自己的回答合同；
6. P02 一次最多补充一条候选洞察；是否符合不同 ToC 用户偏好，需要 A/B 测试；
7. 生成校验失败只重写一次，最终失败会在 Trace 中明确标记，生产环境还需要设计更完整的降级体验。

建议下一阶段优先做：

```text
扩大P02真实问法与Bad Case
→ 人工标注回答满意度和信息偏好
→ 校准默认信息包与显著性阈值
→ 补齐复合意图路由
→ 再决定是否把Answer Plan框架复用到P01/P05
```

---

## 11. GitHub 同步记录

远程仓库：`https://github.com/jinjin12123jin-sketch/aicoach1.0.git`（公开仓库）

同步检查结果：仓库创建于 2026-08-22，当前 size 为 0，没有 commit、branch 引用或旧版本。因此本次只能作为安全处理后的首次提交，无法在 Git 历史中补出一个实际上没有上传过的旧版本。

同步原则：提交完整程序、配置、文档、测试和合成 Demo 数据；绝不提交 `.env`、API Key、本地虚拟环境、缓存、真实睡眠明细和用户运行历史。

同步结果：

```text
分支：main
首次实现提交：562fdd0 (feat: add traceable P02 answer planning workflow)
状态：已成功推送并设置本地 main 跟踪 origin/main
旧版本标签：无；远程此前为空，不存在可标记的历史 commit
```

本机私有文件继续存在于原位置，只是没有进入 Git 索引；GitHub 版本使用合成 Demo 数据运行。
